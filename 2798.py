class Solution:
    def numberOfEmployeesWhoMetTarget(self, hours: List[int], target: int) -> int:
        answer=0
        for i in hours:
            if i>=target:
                answer+=1
        return answer

class Solution:
    def numberOfEmployeesWhoMetTarget(self, hours: List[int], target: int) -> int:
        answer=0
        for i in hours:
            if i>=target:
                answer+=1
        return answer
